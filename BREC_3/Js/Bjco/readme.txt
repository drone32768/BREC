This directory contains a basic java script console like.  

An object of this class is instantiated.  After which user line entries
are provided to a callback.  Output lines can be written to the object.

Passes with:
jslint --eqeq --vars --plusplus --white

