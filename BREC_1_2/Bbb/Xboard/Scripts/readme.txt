
These scripts use various utilities to test the board, fpga, vhdl, C++ device
interfac software and the PRU software.

NOTE: Some test use the pru (and require the device tree to be installed) 
while others use generic cpu gpio and will not work with the device tree
installed (the one for the pru)

